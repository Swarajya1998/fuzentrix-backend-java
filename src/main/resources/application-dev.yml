# Development profile — activate with: spring.profiles.active=dev
# These settings override the base application.yml for local development only.

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
